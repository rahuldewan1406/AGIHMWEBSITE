{
  "name": "agihm-website",
  "version": "1.0.0",
  "private": true,
  "description": "AGIHM hospitality institute website with security-hardening and tests",
  "scripts": {
    "lint": "eslint . --ext .js",
    "test": "node --test",
    "audit": "npm audit --audit-level=high",
    "build": "node scripts/build-check.js"
  },
  "devDependencies": {
    "eslint": "^10.8.1",
    "globals": "^17.11.0"
  }
}
